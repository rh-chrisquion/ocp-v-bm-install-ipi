# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Keep the lock file committed (do NOT ignore .terraform.lock.hcl)

# Local tfvars that may hold real subnet/account IDs — commit terraform.tfvars.example instead
terraform.tfvars
*.auto.tfvars

# JetBrains IDE
**/.idea
