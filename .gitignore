# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Keep the lock file committed (do NOT ignore .terraform.lock.hcl)

# Local tfvars that may hold real subnet/account IDs — commit terraform.tfvars.example instead
terraform.tfvars
*.auto.tfvars

# JetBrains IDE
**/.idea

**/.DS_Store

# OCP bare-metal bootstrap: generated day-0 artifacts and local secret material
# (see sources/ocp-baremetal-bootstrap/README.md "End-to-end workflow")
sources/ocp-baremetal-bootstrap/build/
sources/ocp-baremetal-bootstrap/secrets/
sources/ocp-baremetal-bootstrap/inventories/*/group_vars/*.local.yml
