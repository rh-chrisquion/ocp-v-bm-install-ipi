---
extends: default

ignore: |
  .git/
  sources/*/chart/templates/

rules:
  document-start: disable

  truthy:
    check-keys: false

  line-length:
    max: 120
    level: warning

  indentation:
    indent-sequences: consistent
